# configs/staging.yml — Staging environment overrides
targets:
  staging:
    variables:
      node_type: Standard_D4ds_v4
      autotermination_minutes: 30
      max_workers: 0
      unity_catalog: staging
      schema: clinical_nlp
