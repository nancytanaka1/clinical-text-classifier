# configs/dev.yml — Development environment overrides
targets:
  dev:
    variables:
      node_type: Standard_D4ds_v4
      autotermination_minutes: 15
      max_workers: 0
      unity_catalog: dev
      schema: clinical_nlp
